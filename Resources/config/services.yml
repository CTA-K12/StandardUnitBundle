parameters:
#    mesd_standard_unit.example.class: Mesd\StandardUnitBundle\Example

services:
    MesdStandardUnitBundle.form.type.unit:
        # this is an entity type, it uses those options
        class: Mesd\StandardUnitBundle\FormType\Atom\UnitAtom
        arguments: [ ]
        tags:
            - { name: form.type, alias: unit }

    MesdStandardUnitBundle.form.type.timeunit:
        # this is an entity type, it uses those options
        class: Mesd\StandardUnitBundle\FormType\Atom\TimeUnitAtom
        arguments: [  ]
        tags:
            - { name: form.type, alias: timeunit }
