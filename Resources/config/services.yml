parameters:
#    mesd_standard_unit.example.class: Mesd\Standard\UnitBundle\Example

services:
    MesdStandardUnitBundle.form.type.unit:
        # this is an entity type, it uses those options
        class: Mesd\Standard\UnitBundle\FormType\Atom\UnitAtom
        arguments: [ "@doctrine.orm.entity_manager", "@security.context" ]
        tags:
            - { name: form.type, alias: unit }

    MesdStandardUnitBundle.form.type.timeunit:
        # this is an entity type, it uses those options
        class: Mesd\Standard\UnitBundle\FormType\Atom\TimeUnitAtom
        arguments: [ "@doctrine.orm.entity_manager", "@security.context" ]
        tags:
            - { name: form.type, alias: timeunit }
